------------ LIMIT ------------

/*LIMIT es una cláusula que se usa en consultas SQL para restringir la cantidad de filas que devuelve una consulta.*/

-- Ejemplo -->  
SELECT * from users WHERE email like "%gmail.com" limit 2;