------------ ORDER BY ------------

/*Sirve para ordenar los resultados de una consulta, ya sea de forma ascendente 
(ASC, por defecto) o descendente (DESC).*/

-- Ejemplo --> 
SELECT * FROM users order by age desc; 
--(en el ejemplo estas de forma descendiente pero 
--tambien se puede agregar asc para ordenarlo de forma ascendente.)